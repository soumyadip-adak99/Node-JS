import { asyncHandler } from "../utils/asyncHandler,js";

export const googleLogin = asyncHandler((req, res) => {
    try {
    } catch (error) {
        console.log("Error while google login: ", error);
    }
});
